apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  selector:
    matchLabels:
      app: url-shortener
      tier: backend
      track: stable
  replicas: 3
  template:
    metadata:
      labels:
        app: url-shortener
        tier: backend
        track: stable
    spec:
      containers:
        - name: backend
          image: gcr.io/PROJECT_ID/IMAGE:TAG
          ports:
            - name: http
              containerPort: 80